name = "drift-mail"
main = "src/index.js"
compatibility_date = "2024-01-01"

[assets]
directory = "public"
binding = "ASSETS"

[triggers]
crons = ["*/5 * * * *"]

# 在 Cloudflare Dashboard → Workers → Settings → Bindings 中绑定：
# - D1 数据库：变量名 DB
# - KV 命名空间：变量名 MAIL_KV